# local-dictation-app

Fully local, open-source dictation app for macOS (Linux + Windows in v2).
Inspired by FreeFlow, Wispr Flow, Superwhisper. Zero cloud, zero account, zero telemetry.

**Status:** architecture design completed. Implementation has not started.

## Documentation

- [Architecture design (v1)](docs/specs/2026-05-15-architecture-design.md) - source of truth for cross-cutting decisions.
- Sub-project specs to follow as each module enters implementation (see §10 of the architecture design).

## Project name

The folder name `local-dictation-app` is a placeholder. The product name is intentionally not chosen yet (see "Open decisions" in the architecture design).
